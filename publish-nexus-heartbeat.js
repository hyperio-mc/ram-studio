'use strict';
// publish-nexus-heartbeat.js
// NEXUS — AI Agent Orchestration Platform
// Design Heartbeat — Mar 21, 2026
//
// Inspired by:
//   • LangChain on land-book.com — dark navy, glowing node graph
//   • Capacity on land-book.com — bento-grid capability showcase
//   • Runlayer on land-book.com — deep violet, isometric skill-node connections
//   • Darknode on Awwwards — AI automation workflow canvas aesthetic

const https = require('https');
const fs    = require('fs');
const path  = require('path');

const config       = JSON.parse(fs.readFileSync(path.join(__dirname, 'community-config.json'), 'utf8'));
const GITHUB_TOKEN = config.GITHUB_TOKEN;
const GITHUB_REPO  = config.GITHUB_REPO;

const SLUG    = 'nexus-agent-platform';
const DATE_STR = 'Mar 21, 2026';
const TAGLINE  = 'Orchestrate AI Agents at Scale.';

const P = {
  bg:        '#050810',
  surface:   '#0c0f1e',
  surface2:  '#131728',
  border:    '#1e2845',
  muted:     '#3d4f7a',
  fg2:       '#8090b8',
  fg:        '#dce4f8',
  white:     '#f0f4ff',
  accent:    '#7c5cfc',
  accentLt:  '#9b80ff',
  cyan:      '#3dc9e8',
  teal:      '#22d3a8',
  amber:     '#f0a040',
  red:       '#ff4560',
  green:     '#34d99b',
};

const cssTokens = `/* NEXUS Design Tokens — AI Agent Orchestration Platform */
/* Generated by RAM Design Studio — ${DATE_STR} */

:root {
  /* Background Scale */
  --nexus-bg:        ${P.bg};
  --nexus-surface:   ${P.surface};
  --nexus-surface-2: ${P.surface2};
  --nexus-border:    ${P.border};

  /* Typography */
  --nexus-fg:        ${P.fg};
  --nexus-fg-2:      ${P.fg2};
  --nexus-muted:     ${P.muted};
  --nexus-white:     ${P.white};

  /* Brand Accent */
  --nexus-accent:    ${P.accent};
  --nexus-accent-lt: ${P.accentLt};

  /* Semantic Colors */
  --nexus-cyan:      ${P.cyan};
  --nexus-teal:      ${P.teal};
  --nexus-amber:     ${P.amber};
  --nexus-red:       ${P.red};
  --nexus-green:     ${P.green};

  /* Radius */
  --nexus-r-sm:   8px;
  --nexus-r-md:  12px;
  --nexus-r-lg:  14px;
  --nexus-r-pill: 9999px;

  /* Spacing — 4px base grid */
  --nexus-sp-1:  4px;
  --nexus-sp-2:  8px;
  --nexus-sp-3: 12px;
  --nexus-sp-4: 16px;
  --nexus-sp-5: 20px;
  --nexus-sp-6: 24px;
  --nexus-sp-8: 32px;
  --nexus-sp-12: 48px;
  --nexus-sp-16: 64px;

  /* Typography Scale */
  --nexus-text-xs:   9px;
  --nexus-text-sm:  11px;
  --nexus-text-base: 13px;
  --nexus-text-md:  16px;
  --nexus-text-lg:  20px;
  --nexus-text-xl:  26px;
  --nexus-text-2xl: 36px;
  --nexus-text-hero: 56px;

  /* Shadows / Glow */
  --nexus-glow-accent: 0 0 40px ${P.accent}33;
  --nexus-glow-teal:   0 0 30px ${P.teal}33;
  --nexus-glow-cyan:   0 0 30px ${P.cyan}33;
}`;

const prd = `
<h3>Overview</h3>
<p>NEXUS is an AI Agent Orchestration Platform built for developers and ML teams who need to deploy reliable multi-agent pipelines at scale. Inspired by the visual language emerging across LangChain, Capacity, and Runlayer — deep navy backgrounds, glowing node graphs, bento capability grids — NEXUS makes the complex orchestration of AI agents feel tangible and controllable.</p>
<p>The design challenge was to apply the <strong>bento-grid + node graph</strong> aesthetic seen proliferating in the AI developer tool space (land-book.com, March 2026) to a coherent product experience across mobile and desktop.</p>

<h3>Target Users</h3>
<ul>
  <li><strong>ML engineers</strong> — building research, code review, and data extraction pipelines</li>
  <li><strong>Backend developers</strong> — integrating agent workflows via REST API</li>
  <li><strong>AI product teams</strong> — monitoring agent performance, costs, and reliability</li>
</ul>

<h3>Core Features</h3>
<ul>
  <li><strong>Dashboard</strong> — live KPI strip, active pipeline status, token/latency metrics, event feed</li>
  <li><strong>Build</strong> — bento grid of 7 capability tiles (Web Search, Code Exec, Memory, RAG, APIs, Multi-Agent, Scheduler) with category filtering and search</li>
  <li><strong>Monitor</strong> — live node graph visualization of running pipelines with branch/merge flows, elapsed time, token count, cost tracking</li>
  <li><strong>Desktop Overview</strong> — full-width hero with glowing orbital node diagram, KPI strip, 5-tile capability preview</li>
  <li><strong>Pipeline Builder</strong> — drag canvas with typed node cards (Trigger, LLM Call, Tool Use, Output), right inspector panel with model config</li>
  <li><strong>API Reference</strong> — endpoint sidebar, tabbed code examples (Node.js/Python/curl), response previews, auth key management, rate limit tiers</li>
</ul>

<h3>Design Language</h3>
<p><strong>Node graph as hero element</strong> — the glowing orbital node diagram on the desktop overview isn't decorative; it shows the actual orchestration topology. Every satellite node connects to the central NEXUS orchestrator, communicating the platform's core value visually.</p>
<p><strong>Bento grid for capabilities</strong> — the Build screen's 7-tile grid (inspired by Capacity's product bento on land-book.com) lets users scan all capabilities at a glance. Each tile has a top accent line in its semantic color, an icon circle, title, and description — creating visual variety without chaos.</p>
<p><strong>Color as semantic signal</strong> — Violet (#7c5cfc) for primary actions and the orchestrator node; Teal (#22d3a8) for active/running states; Cyan (#3dc9e8) for data/retrieval nodes; Amber (#f0a040) for caution/streaming; Green (#34d99b) for success and health. No color is decorative.</p>

<h3>Screen Architecture</h3>
<ol>
  <li><strong>Mobile Dashboard</strong> (375×812) — KPI strip, live pipelines with progress bars, event feed, bottom nav</li>
  <li><strong>Mobile Build</strong> (375×812) — search bar, category tabs, 2×3+1 bento capability grid, create CTA</li>
  <li><strong>Mobile Monitor</strong> (375×812) — 6-node pipeline graph with Trigger→Planner→(Search|Code)→Writer→Output flow, run stats, controls</li>
  <li><strong>Desktop Overview</strong> (1440×900) — hero copy, glowing orbital node diagram, 5 bento tiles at bottom</li>
  <li><strong>Desktop Pipeline Builder</strong> (1440×900) — left component sidebar, center canvas with 4 node cards + connectors, right inspector panel</li>
  <li><strong>Desktop API Reference</strong> (1440×900) — 3-column: endpoint list, code panel with response, auth + params panel</li>
</ol>`;

// ── SVG thumbnail renderer ────────────────────────────────────────────────────
function renderElSVG(el, depth) {
  if (!el || depth > 6) return '';
  const x = el.x || 0, y = el.y || 0;
  const w = Math.max(0, el.width || 0), h = Math.max(0, el.height || 0);
  const fill = el.fill || 'none';
  const oAttr = (el.opacity !== undefined && el.opacity < 0.99) ? ` opacity="${el.opacity.toFixed(2)}"` : '';
  const rAttr = el.cornerRadius ? ` rx="${Math.min(el.cornerRadius, w/2, h/2)}"` : '';

  if (el.type === 'frame') {
    const bg   = `<rect x="${x}" y="${y}" width="${w}" height="${h}" fill="${fill}"${rAttr}${oAttr}/>`;
    const kids = (el.children || []).map(c => renderElSVG(c, depth + 1)).join('');
    if (!kids) return bg;
    const clipId = `clip-${el.id}`;
    if (el.clip) {
      return `${bg}<clipPath id="${clipId}"><rect x="${x}" y="${y}" width="${w}" height="${h}"${rAttr}/></clipPath><g clip-path="url(#${clipId})" transform="translate(${x},${y})">${kids}</g>`;
    }
    return `${bg}<g transform="translate(${x},${y})">${kids}</g>`;
  }
  if (el.type === 'ellipse') {
    const rx = w/2, ry = h/2;
    return `<ellipse cx="${x+rx}" cy="${y+ry}" rx="${rx}" ry="${ry}" fill="${fill}"${oAttr}/>`;
  }
  if (el.type === 'text') {
    const fh = Math.max(1, Math.min(h, (el.fontSize || 13) * 0.65));
    return `<rect x="${x}" y="${y+(h-fh)/2}" width="${w}" height="${fh}" fill="${fill}"${oAttr} rx="1"/>`;
  }
  return '';
}

function screenThumbSVG(screen, tw, th) {
  const sw = screen.width, sh = screen.height;
  const scale = Math.min(tw / sw, th / sh);
  const dw = sw * scale, dh = sh * scale;
  const ox = (tw - dw) / 2, oy = (th - dh) / 2;
  const kids = (screen.children || []).map(c => renderElSVG(c, 0)).join('');
  return `<svg xmlns="http://www.w3.org/2000/svg" width="${tw}" height="${th}" style="background:${P.surface}">
  <g transform="translate(${ox},${oy}) scale(${scale.toFixed(4)})">
    <rect width="${sw}" height="${sh}" fill="${screen.fill || P.bg}"/>
    ${kids}
  </g>
</svg>`;
}

// ── Hero HTML builder ─────────────────────────────────────────────────────────
function buildHeroHTML(pen, penJson) {
  const screens = pen.children;
  const thumbW = 280, thumbH = 200;
  const thumbsSVG = screens.map((s, i) => {
    const svg = screenThumbSVG(s, thumbW, thumbH);
    const b64 = Buffer.from(svg).toString('base64');
    const label = ['Mobile Dashboard','Mobile Build','Mobile Monitor',
                   'Desktop Overview','Desktop Builder','API Reference'][i] || `Screen ${i+1}`;
    return `
    <div class="thumb-item">
      <div class="thumb-frame">
        <img src="data:image/svg+xml;base64,${b64}" width="${thumbW}" height="${thumbH}" alt="${label}" loading="lazy"/>
      </div>
      <div class="thumb-label">${label}</div>
    </div>`;
  }).join('');

  const penEscaped = penJson
    .replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/'/g,'&#39;');

  return `<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>NEXUS — AI Agent Orchestration Platform · RAM Design Studio</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=JetBrains+Mono:wght@400;500;600&display=swap');
  *{box-sizing:border-box;margin:0;padding:0}
  :root{
    --bg:${P.bg}; --surf:${P.surface}; --surf2:${P.surface2};
    --border:${P.border}; --muted:${P.muted}; --fg2:${P.fg2}; --fg:${P.fg};
    --accent:${P.accent}; --accentLt:${P.accentLt};
    --cyan:${P.cyan}; --teal:${P.teal}; --amber:${P.amber};
    --red:${P.red}; --green:${P.green};
  }
  body{background:var(--bg);color:var(--fg);font-family:'Inter',sans-serif;line-height:1.6;min-height:100vh;overflow-x:hidden}
  #toast{position:fixed;bottom:24px;left:50%;transform:translateX(-50%);background:var(--accent);color:#fff;padding:10px 20px;border-radius:20px;font-size:13px;font-weight:600;opacity:0;transition:opacity .3s;pointer-events:none;z-index:9999}
  #toast.show{opacity:1}

  /* Hero */
  .hero{position:relative;padding:80px 48px 60px;overflow:hidden;border-bottom:1px solid var(--border)}
  .hero::before{content:'';position:absolute;top:-80px;right:-80px;width:600px;height:600px;border-radius:50%;background:radial-gradient(circle,${P.accent}18 0%,transparent 70%);pointer-events:none}
  .hero::after{content:'';position:absolute;bottom:-80px;left:-80px;width:400px;height:400px;border-radius:50%;background:radial-gradient(circle,${P.teal}12 0%,transparent 70%);pointer-events:none}
  .hero-eyebrow{font-size:9px;font-weight:700;letter-spacing:3px;color:var(--accent);text-transform:uppercase;margin-bottom:20px;display:flex;align-items:center;gap:10px}
  .hero-eyebrow::before{content:'';display:inline-block;width:24px;height:1px;background:var(--accent)}
  .hero-name{font-size:72px;font-weight:900;letter-spacing:-2px;line-height:1;color:${P.white};margin-bottom:12px}
  .hero-tagline{font-size:28px;font-weight:300;color:var(--accentLt);letter-spacing:-0.5px;margin-bottom:28px}
  .hero-desc{font-size:16px;color:var(--fg2);max-width:580px;line-height:1.75;margin-bottom:36px}
  .btn-row{display:flex;flex-wrap:wrap;gap:12px;margin-bottom:48px}
  .btn{display:inline-flex;align-items:center;gap:8px;padding:12px 22px;border-radius:10px;font-size:13px;font-weight:600;text-decoration:none;cursor:pointer;border:none;transition:all .2s}
  .btn-primary{background:var(--accent);color:#fff}
  .btn-primary:hover{background:var(--accentLt)}
  .btn-outline{background:transparent;color:var(--fg);border:1px solid var(--border2,var(--border))}
  .btn-outline:hover{border-color:var(--accent);color:var(--accent)}
  .btn-ghost{background:var(--surf2);color:var(--fg2)}
  .btn-ghost:hover{color:var(--fg)}
  .meta-chips{display:flex;flex-wrap:wrap;gap:8px}
  .chip{display:inline-flex;align-items:center;gap:6px;padding:5px 12px;border-radius:99px;font-size:10px;font-weight:700;letter-spacing:0.8px;border:1px solid}
  .chip-accent{color:var(--accent);border-color:${P.accent}44;background:${P.accent}15}
  .chip-teal{color:var(--teal);border-color:${P.teal}44;background:${P.teal}15}
  .chip-cyan{color:var(--cyan);border-color:${P.cyan}44;background:${P.cyan}15}
  .chip-green{color:var(--green);border-color:${P.green}44;background:${P.green}15}

  /* Screens */
  .screens-section{padding:60px 48px;border-bottom:1px solid var(--border)}
  .section-label{font-size:9px;font-weight:700;letter-spacing:3px;text-transform:uppercase;color:var(--muted);margin-bottom:32px}
  .thumbs-strip{display:flex;gap:20px;overflow-x:auto;padding-bottom:16px}
  .thumbs-strip::-webkit-scrollbar{height:4px}
  .thumbs-strip::-webkit-scrollbar-track{background:var(--surf)}
  .thumbs-strip::-webkit-scrollbar-thumb{background:var(--accent)60;border-radius:2px}
  .thumb-item{flex:0 0 auto;text-align:center}
  .thumb-frame{border-radius:10px;overflow:hidden;border:1px solid var(--border);background:var(--surf)}
  .thumb-frame img{display:block}
  .thumb-label{margin-top:8px;font-size:10px;font-weight:600;color:var(--fg2);letter-spacing:0.5px}

  /* Brand spec */
  .brand-section{padding:60px 48px;border-bottom:1px solid var(--border);display:grid;grid-template-columns:repeat(auto-fit,minmax(300px,1fr));gap:48px}
  .brand-block h3{font-size:9px;font-weight:700;letter-spacing:3px;color:var(--muted);text-transform:uppercase;margin-bottom:20px}
  .palette-row{display:flex;gap:10px;flex-wrap:wrap}
  .swatch{display:flex;flex-direction:column;align-items:center;gap:6px}
  .swatch-color{width:56px;height:56px;border-radius:10px;border:1px solid rgba(255,255,255,.08)}
  .swatch-hex{font-size:9px;font-family:'JetBrains Mono',monospace;color:var(--fg2)}
  .swatch-name{font-size:9px;color:var(--muted);letter-spacing:0.5px}
  .type-row{display:flex;flex-direction:column;gap:10px}
  .type-ex{display:flex;align-items:baseline;gap:14px}
  .type-size{font-size:9px;color:var(--muted);font-family:'JetBrains Mono',monospace;flex:0 0 40px}
  .space-row{display:flex;flex-direction:column;gap:8px}
  .space-ex{display:flex;align-items:center;gap:12px}
  .space-px{font-size:9px;font-family:'JetBrains Mono',monospace;color:var(--muted);flex:0 0 36px}
  .principles-list{display:flex;flex-direction:column;gap:16px}
  .principle{display:flex;gap:14px;align-items:flex-start}
  .principle-num{color:var(--accent);font-size:10px;font-weight:700;flex:0 0 20px;margin-top:2px}
  .principle-text{font-size:13px;color:var(--fg2);line-height:1.6}

  /* Tokens */
  .tokens-section{padding:60px 48px;border-bottom:1px solid var(--border);background:var(--surf)}
  .tokens-block{position:relative}
  .copy-btn{position:absolute;top:16px;right:16px;background:var(--accent);color:#fff;border:none;padding:8px 16px;border-radius:8px;font-size:10px;font-weight:700;letter-spacing:1px;cursor:pointer;z-index:10}
  .copy-btn:hover{background:var(--accentLt)}
  .tokens-pre{background:var(--surf2);border:1px solid var(--border);border-radius:12px;padding:48px 24px 24px;font-family:'JetBrains Mono',monospace;font-size:12px;color:var(--fg2);overflow-x:auto;line-height:1.7;max-height:400px;overflow-y:auto}

  /* Prompt & PRD */
  .prompt-section{padding:60px 48px;border-bottom:1px solid var(--border);background:${P.accent}08}
  .p-label{font-size:9px;font-weight:700;letter-spacing:3px;color:var(--accent);text-transform:uppercase;margin-bottom:20px}
  .p-text{font-size:20px;font-style:italic;color:var(--fg);line-height:1.7;max-width:860px;opacity:.9}
  .prd-section{padding:60px 48px;border-bottom:1px solid var(--border)}
  .prd-section h3{font-size:13px;font-weight:700;letter-spacing:1px;color:var(--accent);text-transform:uppercase;margin:28px 0 10px;border-left:2px solid var(--accent);padding-left:12px}
  .prd-section h3:first-child{margin-top:0}
  .prd-section p,.prd-section li{font-size:14px;color:var(--fg2);line-height:1.75;margin-bottom:8px}
  .prd-section ul,.prd-section ol{padding-left:20px}
  .prd-section strong{color:var(--fg);font-weight:600}

  footer{padding:40px 48px;border-top:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;font-size:11px;color:var(--muted)}
</style>
</head>
<body>
<div id="toast"></div>

<section class="hero">
  <div class="hero-eyebrow">RAM Design Studio · Design Heartbeat · ${DATE_STR}</div>
  <div class="hero-name">NEXUS</div>
  <div class="hero-tagline">${TAGLINE}</div>
  <div class="hero-desc">A dark-mode AI Agent Orchestration Platform — featuring bento-grid capability tiles, glowing node pipeline visualization, and a full developer API experience. Inspired by the AI developer tool aesthetic emerging across land-book.com (LangChain, Capacity, Runlayer).</div>
  <div class="btn-row">
    <button class="btn btn-primary" onclick="openViewer()">☰ Open in Viewer</button>
    <button class="btn btn-outline" onclick="downloadPen()">⬇ Download .pen</button>
    <button class="btn btn-ghost" onclick="copyPrompt()">⎘ Copy Prompt</button>
    <button class="btn btn-ghost" onclick="shareOnX()">𝕏 Share</button>
    <a href="https://ram.zenbin.org/gallery" class="btn btn-ghost">← Gallery</a>
  </div>
  <div class="meta-chips">
    <span class="chip chip-accent">6 SCREENS</span>
    <span class="chip chip-teal">MOBILE + DESKTOP</span>
    <span class="chip chip-cyan">AI / DEV TOOLS</span>
    <span class="chip chip-green">DARK MODE</span>
  </div>
</section>

<section class="screens-section">
  <div class="section-label">All Screens</div>
  <div class="thumbs-strip">${thumbsSVG}</div>
</section>

<section class="brand-section">
  <div class="brand-block">
    <h3>Color Palette</h3>
    <div class="palette-row">
      ${[
        [P.bg,       'bg',      'Near-Void'],
        [P.surface,  'surface', 'Surface'],
        [P.surface2, 'surf-2',  'Elevated'],
        [P.accent,   'accent',  'Violet'],
        [P.accentLt, 'acc-lt',  'Violet Lt'],
        [P.cyan,     'cyan',    'Cyan'],
        [P.teal,     'teal',    'Teal'],
        [P.amber,    'amber',   'Amber'],
        [P.green,    'green',   'Green'],
        [P.red,      'red',     'Red'],
      ].map(([c, k, n]) => `
      <div class="swatch">
        <div class="swatch-color" style="background:${c}"></div>
        <div class="swatch-hex">${c}</div>
        <div class="swatch-name">${n}</div>
      </div>`).join('')}
    </div>
  </div>

  <div class="brand-block">
    <h3>Type Scale</h3>
    <div class="type-row">
      ${[['9px',400,'LABEL / META','letter-spacing:2px'],['11px',400,'Body Small'],['13px',400,'Body'],['16px',600,'Body Emphasis'],['20px',700,'Subheading'],['26px',800,'Stat Value'],['56px',900,'HERO']].map(([sz,wt,lbl,extra=''])=>`
      <div class="type-ex">
        <span class="type-size">${sz}</span>
        <span style="font-size:${sz};font-weight:${wt};color:${P.fg};${extra}">${lbl}</span>
      </div>`).join('')}
    </div>
  </div>

  <div class="brand-block">
    <h3>Spacing · 4px Base Grid</h3>
    <div class="space-row">
      ${[4,8,12,16,24,32,48,64].map(sp=>`
      <div class="space-ex">
        <span class="space-px">${sp}px</span>
        <div style="height:8px;border-radius:4px;background:${P.accent};width:${sp*2}px;opacity:.7"></div>
      </div>`).join('')}
    </div>
  </div>

  <div class="brand-block">
    <h3>Design Principles</h3>
    <div class="principles-list">
      <div class="principle">
        <span class="principle-num">01</span>
        <span class="principle-text">Node graph as truth — the glowing orbital diagram on the overview shows actual agent topology, not illustration. Structure communicates function.</span>
      </div>
      <div class="principle">
        <span class="principle-num">02</span>
        <span class="principle-text">Color as semantic signal — violet for orchestration, teal for active states, cyan for retrieval, amber for caution, green for health. Never decorative.</span>
      </div>
      <div class="principle">
        <span class="principle-num">03</span>
        <span class="principle-text">Bento grid for capability scanning — the Build screen's tile grid lets developers assess the full capability surface in one glance, without reading menus.</span>
      </div>
    </div>
  </div>
</section>

<section class="tokens-section">
  <div class="section-label">CSS Design Tokens</div>
  <div class="tokens-block">
    <button class="copy-btn" onclick="copyTokens()">COPY TOKENS</button>
    <pre class="tokens-pre" id="tokens-pre">${cssTokens.replace(/</g,'&lt;')}</pre>
  </div>
</section>

<section class="prompt-section">
  <div class="p-label">Original Prompt</div>
  <p class="p-text">"Design NEXUS — an AI Agent Orchestration Platform — inspired by the bento-grid capability showcase + glowing node graph aesthetic seen across LangChain, Capacity, and Runlayer on land-book.com. Dark navy (#050810), electric violet accent (#7c5cfc). 6 screens: mobile dashboard, mobile build (bento tiles), mobile monitor (node graph), desktop overview (orbital node diagram hero), desktop pipeline builder (canvas), desktop API reference."</p>
</section>

<section class="prd-section">
  <div class="p-label">Product Brief</div>
  ${prd}
</section>

<footer>
  <span>RAM Design Studio · Design Heartbeat · ${DATE_STR}</span>
  <a href="https://ram.zenbin.org/gallery" style="color:inherit;opacity:.6">ram.zenbin.org/gallery</a>
</footer>

<script type="application/json" id="pen-data">${penEscaped}</script>
<script>
function getPen(){return document.getElementById('pen-data').textContent.trim().replace(/&amp;/g,'&').replace(/&lt;/g,'<').replace(/&gt;/g,'>').replace(/&#39;/g,"'")}
function downloadPen(){const b=new Blob([getPen()],{type:'application/json'});const u=URL.createObjectURL(b);const a=document.createElement('a');a.href=u;a.download='nexus-app.pen';document.body.appendChild(a);a.click();document.body.removeChild(a);URL.revokeObjectURL(u)}
function openViewer(){window.open('https://ram.zenbin.org/${SLUG}-viewer','_blank')}
function copyPrompt(){navigator.clipboard.writeText('Design NEXUS — an AI Agent Orchestration Platform — inspired by the bento-grid capability showcase + glowing node graph aesthetic seen across LangChain, Capacity, and Runlayer on land-book.com. Dark navy, electric violet accent. 6 screens: mobile dashboard, mobile build bento tiles, mobile monitor node graph, desktop overview with orbital node diagram hero, desktop pipeline builder canvas, desktop API reference.').then(()=>toast('Prompt Copied ✓'))}
function copyTokens(){navigator.clipboard.writeText(document.getElementById('tokens-pre').textContent).then(()=>toast('Tokens Copied ✓'))}
function shareOnX(){const text=encodeURIComponent('NEXUS — AI Agent Orchestration Platform. Dark navy + electric violet. Bento grid + node graph aesthetic. 6 screens + brand spec + CSS tokens. Built by RAM Design Studio.');const url=encodeURIComponent(window.location.href);window.open('https://x.com/intent/tweet?text='+text+'&url='+url,'_blank')}
function toast(msg){const t=document.getElementById('toast');t.textContent=msg;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2200)}
</script>
</body>
</html>`;
}

// ── HTTP helpers ──────────────────────────────────────────────────────────────
function post(slug, title, html, subdomain) {
  return new Promise((resolve) => {
    const body = JSON.stringify({ title, html });
    const opts = {
      hostname: 'zenbin.org',
      path: '/v1/pages/' + slug,
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Content-Length': Buffer.byteLength(body),
        ...(subdomain ? { 'X-Subdomain': subdomain } : {}),
      },
    };
    const req = https.request(opts, res => {
      let d = '';
      res.on('data', c => d += c);
      res.on('end', () => resolve({ status: res.statusCode, body: d }));
    });
    req.on('error', e => resolve({ status: 0, error: e.message }));
    req.write(body);
    req.end();
  });
}

async function getQueueSha() {
  const r = await new Promise((resolve) => {
    const opts = {
      hostname: 'api.github.com',
      path: `/repos/${GITHUB_REPO}/contents/queue.json`,
      method: 'GET',
      headers: {
        'Authorization': `token ${GITHUB_TOKEN}`,
        'User-Agent': 'design-studio-agent/1.0',
        'Accept': 'application/vnd.github.v3+json',
      },
    };
    const req = https.request(opts, res => {
      let d = '';
      res.on('data', c => d += c);
      res.on('end', () => resolve({ status: res.statusCode, body: d }));
    });
    req.on('error', e => resolve({ status: 0, error: e.message }));
    req.end();
  });
  if (r.status !== 200) throw new Error('Cannot get SHA: ' + r.status);
  return JSON.parse(r.body).sha;
}

async function pushGalleryEntry(entry) {
  let queue;
  try {
    const raw = await new Promise((resolve) => {
      const opts = {
        hostname: 'raw.githubusercontent.com',
        path: `/${GITHUB_REPO}/main/queue.json`,
        method: 'GET',
        headers: { 'User-Agent': 'design-studio-agent/1.0' },
      };
      const req = https.request(opts, res => {
        let d = '';
        res.on('data', c => d += c);
        res.on('end', () => resolve(d));
      });
      req.on('error', () => resolve('{"submissions":[]}'));
      req.end();
    });
    queue = JSON.parse(raw);
  } catch { queue = { submissions: [] }; }

  queue.submissions.push(entry);
  const sha = await getQueueSha();
  const content = Buffer.from(JSON.stringify(queue, null, 2)).toString('base64');
  const body = JSON.stringify({
    message: `add: nexus-heartbeat — AI agent orchestration platform design`,
    content,
    sha,
  });
  return new Promise((resolve) => {
    const opts = {
      hostname: 'api.github.com',
      path: `/repos/${GITHUB_REPO}/contents/queue.json`,
      method: 'PUT',
      headers: {
        'Authorization': `token ${GITHUB_TOKEN}`,
        'User-Agent': 'design-studio-agent/1.0',
        'Content-Type': 'application/json',
        'Content-Length': Buffer.byteLength(body),
        'Accept': 'application/vnd.github.v3+json',
      },
    };
    const req = https.request(opts, res => {
      let d = '';
      res.on('data', c => d += c);
      res.on('end', () => resolve({ status: res.statusCode, body: d }));
    });
    req.on('error', e => resolve({ status: 0, error: e.message }));
    req.write(body);
    req.end();
  });
}

// ── Main ──────────────────────────────────────────────────────────────────────
(async () => {
  console.log('\n── NEXUS Heartbeat Publisher ──────────────────');

  const penPath = path.join(__dirname, 'nexus2.pen');
  if (!fs.existsSync(penPath)) {
    console.error('nexus2.pen not found — run: node nexus2-app.js');
    process.exit(1);
  }
  const penJson = fs.readFileSync(penPath, 'utf8');
  const pen = JSON.parse(penJson);
  console.log(`✓ Loaded nexus2.pen: ${pen.children.length} screens`);

  // Build hero HTML
  const heroHTML = buildHeroHTML(pen, penJson);
  console.log(`  Hero HTML: ${(heroHTML.length / 1024).toFixed(1)} KB`);

  // Build viewer HTML
  let viewerHtml = fs.readFileSync(path.join(__dirname, 'penviewer-app.html'), 'utf8');
  const injection = `<script>window.EMBEDDED_PEN = ${JSON.stringify(penJson)};<\/script>`;
  viewerHtml = viewerHtml.replace('<script>', injection + '\n<script>');
  console.log(`  Viewer HTML: ${(viewerHtml.length / 1024).toFixed(1)} KB`);

  // Publish hero
  console.log('\n[1/3] Publishing hero page...');
  let heroSlug = SLUG;
  let r = await post(heroSlug, `NEXUS — AI Agent Orchestration Platform · RAM Design Studio`, heroHTML, 'ram');
  if (r.status === 409) { heroSlug = SLUG + '-2'; r = await post(heroSlug, `NEXUS — AI Agent Orchestration · RAM Design Studio`, heroHTML, 'ram'); }
  if (r.status === 409) { heroSlug = SLUG + '-3'; r = await post(heroSlug, `NEXUS — AI Agent Orchestration · RAM Design Studio`, heroHTML, 'ram'); }
  console.log(`  HTTP ${r.status} → https://ram.zenbin.org/${heroSlug}`);
  const heroUrl = `https://ram.zenbin.org/${heroSlug}`;

  // Publish viewer
  console.log('\n[2/3] Publishing viewer...');
  let viewSlug = SLUG + '-viewer';
  let rv = await post(viewSlug, `NEXUS — Viewer`, viewerHtml, 'ram');
  if (rv.status === 409) { viewSlug = SLUG + '-viewer-2'; rv = await post(viewSlug, `NEXUS — Viewer`, viewerHtml, 'ram'); }
  console.log(`  HTTP ${rv.status} → https://ram.zenbin.org/${viewSlug}`);

  // Gallery queue
  console.log('\n[3/3] Pushing to gallery queue...');
  try {
    const qr = await pushGalleryEntry({
      id: 'heartbeat-nexus-' + Date.now(),
      status: 'done',
      submitted_at: new Date().toISOString(),
      prompt: 'Design NEXUS — an AI Agent Orchestration Platform inspired by bento-grid + glowing node graph aesthetic from land-book.com (LangChain, Capacity, Runlayer). Dark navy, electric violet. 6 screens.',
      credit: 'RAM Design Studio (Heartbeat)',
      design_url: heroUrl,
      viewer_url: `https://ram.zenbin.org/${viewSlug}`,
      app_name: 'NEXUS',
      tagline: TAGLINE,
      archetype: 'dark-ai-developer-tools',
      screens: 6,
    });
    console.log(`  HTTP ${qr.status}`);
  } catch (e) {
    console.log('  Gallery push error:', e.message);
  }

  console.log('\n══ Done ══════════════════════════════════════');
  console.log(`Hero:    ${heroUrl}`);
  console.log(`Viewer:  https://ram.zenbin.org/${viewSlug}`);
  console.log(`Gallery: https://ram.zenbin.org/gallery`);
})();
